# Core dependencies for Taxi Dispatch Multi-Agent System
spade>=3.2.0
ortools>=9.5.0
requests>=2.28.0
psutil>=5.9.0

# Optional: For enhanced logging and async operations  
aiohttp>=3.8.0

# Development and testing dependencies (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Note: tkinter is included with Python standard library
# Note: asyncio, threading, json, time, logging, uuid, enum, typing, dataclasses are all standard library
